<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <link href="<%= url('/style.css') %>" rel="stylesheet" type="text/css" />
    <title><%= @title %></title>
  </head>
  <body>
    <h1><%= @content %></h1>
    <%# メモの内容を表示する。削除・編集へのリンクを作る %>
      <h2><%= @memo_title %></h2>
      <p class="memo_text"><%= @text %></p>
      <div class="button_wrapper">
        <button type="submit" value="変更" class="form_btn"><a href="/memos/<%= @memo_title %>/edit">変更</a></button>
        <form method="POST"  action="/memos/<%= @memo_title %>">
          <input type="hidden" name="_method" value="DELETE"></input>
          <button type="submit"class="form_btn">削除</button>
        </form>
      </div> 
  </body>
</html>