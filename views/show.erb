<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <link href="<%= url('/style.css') %>" rel="stylesheet" type="text/css" />
  <title><%= @title %></title>
</head>
<body class="container">
  <h1><%= @content %></h1>
<%# メモの内容を表示する。削除・編集へのリンクを作る %>
  <p class="memo_text"><%= @text %></p>
  <button type="submit" value="edit" class="form_btn"><a href="/memos/<%= @memo_title %>/edit">編集</a></button>
  <form method="POST"  action="/memos/<%= @memo_title %>">
    <input type="hidden" name="_method" value="DELETE"></input>
    <button type="submit">削除</button>
  </form>
</body>
</html>